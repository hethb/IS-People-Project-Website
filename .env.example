# Copy to `.env` for local development. Never commit a real API key.
# Google Cloud Console: enable "Maps JavaScript API" and restrict key by HTTP referrer (e.g. http://localhost:5173/*).
VITE_GOOGLE_MAPS_API_KEY=
